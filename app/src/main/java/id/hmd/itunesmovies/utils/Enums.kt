package id.hmd.itunesmovies.utils

enum class EnumStage {
    STAGE_DEV, STAGE_SIT, STAGE_UAT, STAGE_PROD
}

enum class ProfileMenuEnums {
    DETAILPROFILE, MYCARD, FAQ, TNC, CONTACTUS, RATEUS, LOGOUT, DIVIDER
}

enum class HistoryMode {
    MODE_TRANSACTION, MODE_VOUCHER_EXPIRED
}

enum class TEST {
    ONE, TWO, THREE
}